pipeline {
    agent any
    stages {
        stage('Current build supported:') {
            steps {
                echo "Running BUild no. ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}
