pipeline {
    agent any
    stages {
        stage('Current build supported:') {
            steps {
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}
